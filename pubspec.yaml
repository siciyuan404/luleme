name: luleme
description: "撸了么 - 提醒你到时间撸猫了 🐱"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # 状态管理
  provider: ^6.1.2
  
  # 本地通知
  flutter_local_notifications: ^18.0.1
  timezone: ^0.10.0
  
  # 本地存储
  shared_preferences: ^2.3.4
  sqflite: ^2.4.1
  
  # 网络请求
  dio: ^5.7.0
  
  # 图片缓存
  cached_network_image: ^3.4.1
  
  # 工具类
  intl: ^0.20.1
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
